<!DOCTYPE html>
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"><html>
  <head>
    <title>Image Search Abstraction Layer</title>
    <meta name="description" content="An app that queries a search API for images information">
    <link id="favicon" rel="icon" href="https://upload.wikimedia.org/wikipedia/commons/thumb/f/fa/Feedbin-Favicon-search.svg/2000px-Feedbin-Favicon-search.svg.png" type="image/x-icon">
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <header>
      <h1>
        Image Search Abstraction Layer
      </h1>
    </header>

    <main>
      <h2>User stories:</h2>
      <ul>
        <li>I can get the image URLs, alt text and page urls for a set of images relating to a given search string.</li>
        <li>I can paginate through the responses by adding a <code>?offset=2</code> parameter to the URL. (In this case, use the offset input instead)</li>
        <li>I can get a list of the most recently submitted search strings.</li>
      </ul>
    </main>
      
    <form action="/" method="post" name="img_search" id="img_search">
      <div class="row">
        <div class="col-md-8">
          <input type="text" required id="text_val" name="text_val" placeholder="write something">
        </div>
        <div class="col-md-2">
          <input type="number" id="offset" name="offset" min="0" placeholder="offset">
        </div>
        <div class="col-md-2">
          <button type="submit" id="search" class="btn btn-primary">Search!</button>
        </div>
      </div>
    </form>

    <% if(find !== null){ %>
      <h1>Search Results</h1>
      <p>[</p>
      <% for(var i = 0; i < find.length; i++){ %>
        <p><%- JSON.stringify(find[i]) %></p>
      <% } %>
      <p>]</p>
    <% } %>

    <% if(recent.length){ %>
      <div>
        <h1>Most recently submitted searches</h1>
        <div>
          <p>[</p>
          <% for(var i = 0; i < recent.length; i++){ %>
            <p><%- JSON.stringify(recent[i]) %></p>
          <% } %>
          <p>]</p>
        </div>
      </div>
    <% } %>

  </body>
</html>